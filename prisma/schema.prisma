generator client {
<<<<<<< Updated upstream
  provider = "prisma-client-js"
=======
  provider = "prisma-client-js" // مولد كود التعامل مع قاعدة البيانات
}
generator client {
  provider = "prisma-client-js" // مولد كود التعامل مع قاعدة البيانات
}
datasource db {
  provider = "postgresql" // نوع قاعدة البيانات: بوستجري
  url      = env("DATABASE_URL") // رابط الاتصال بقاعدة البيانات
}

// ضع النماذج (الجداول) هنا فقط

model User {
  id        String    @id @default(cuid())
  name      String
  email     String    @unique
  password  String
  role      String    @default("user")
  services  Service[]
  orders    Order[]   @relation("UserOrders")
  createdAt DateTime  @default(now())
  verificationRequests VerificationRequest[]
}

model Service {
  id          String   @id @default(cuid())
  title       String
  description String
  price       Float
  image       String?
  images      String[] // قائمة روابط الصور
  userId      String?
  user        User?   @relation(fields: [userId], references: [id]) // علاقة مع المستخدم
  vendorId    String?
  orders      Order[]
  createdAt   DateTime @default(now())
  deliveryTime Int?
}

model Order {
  id        String   @id @default(cuid())
  title     String
  price     Float
  serviceId String
  service   Service  @relation(fields: [serviceId], references: [id])
  userId    String
  user      User     @relation("UserOrders", fields: [userId], references: [id])
  createdAt DateTime @default(now())
}

model IdentitySession {
  id          String   @id @default(cuid())
  userId      String?
  status      String   @default("pending")
  step        String   @default("basic")
  frontUrl    String?
  backUrl     String?
  selfieUrl   String?
  ocrResult   Json?
  flags       Json?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([userId])
}

model Fingerprint {
  id        String   @id @default(cuid())
  fpHash    String   @unique
  ip        String?
  ua        String?
  meta      Json?
  createdAt DateTime @default(now())
}

model AuditLog {
  id        String   @id @default(cuid())
  type      String
  userId    String?
  meta      Json
  createdAt DateTime @default(now())
}

model VerificationRequest {
  id          String   @id @default(cuid())
  userId      String
  fullName    String
  nationalId  String
  dob         DateTime
  selfiePath  String
  idCardPath  String
  status      String   @default("pending")
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  user User @relation(fields: [userId], references: [id])
}
// ASSISTANT_FINAL: true
  id        String    @id @default(cuid())
  name      String
  email     String    @unique
  password  String
  role      String    @default("user")
  services  Service[]
  orders    Order[]   @relation("UserOrders")
  createdAt DateTime  @default(now())
}

model Service {
  id          String   @id @default(cuid())
  title       String
  description String
  price       Float
  userId      String
  user        User     @relation(fields: [userId], references: [id])
  orders      Order[]
  createdAt   DateTime @default(now())
}

model Order {
  id        String   @id @default(cuid())
  title     String
  price     Float
<<<<<<< Updated upstream
  details   String?
=======
>>>>>>> Stashed changes
  serviceId String
  service   Service  @relation(fields: [serviceId], references: [id])
  userId    String
  user      User     @relation("UserOrders", fields: [userId], references: [id])
  createdAt DateTime @default(now())
}

model IdentitySession {
  id          String   @id @default(cuid())
  userId      String?
<<<<<<< Updated upstream
  status      String   @default("pending") // pending | verified | rejected | manual_review
  step        String   @default("basic")   // basic | front | back | selfie | verify
=======
  status      String   @default("pending")
  step        String   @default("basic")
>>>>>>> Stashed changes
  frontUrl    String?
  backUrl     String?
  selfieUrl   String?
  ocrResult   Json?
  flags       Json?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([userId])
}

model Fingerprint {
  id        String   @id @default(cuid())
  fpHash    String   @unique
  ip        String?
  ua        String?
  meta      Json?
  createdAt DateTime @default(now())
}

model AuditLog {
  id        String   @id @default(cuid())
  type      String
  userId    String?
  meta      Json
  createdAt DateTime @default(now())
}

model VerificationRequest {
  id          String   @id @default(cuid())
  userId      String
  fullName    String
  nationalId  String
  dob         DateTime
  selfiePath  String
  idCardPath  String
<<<<<<< Updated upstream
  status      String   @default("pending") // pending | approved | rejected
=======
  status      String   @default("pending")
>>>>>>> Stashed changes
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  user User @relation(fields: [userId], references: [id])
}
<<<<<<< Updated upstream
=======
// ASSISTANT_FINAL: true
>>>>>>> Stashed changes
